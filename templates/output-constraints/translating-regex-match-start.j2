* Translate `match.start()` in Python to `match.index` in JavaScript, which gives the starting index of the match in the string.
  Similarly, `match2.start()` translates to `match2.index`, and so on for any match object.