* In JavaScript translations, always use `var` for variable declarations.
  Do not use `let` or `const` for variable declarations.
  Use `var` even inside for loops (e.g. `for (var i = 0; i < n; i++)` instead of `for (let i = 0; i < n; i++)`).